var carPooling = function (
    trips: number[][],
    capacity: number
) {
    // iterate thru trips
    for (let i = 0; i < trips.length; i++) {
        // num_passengers, start_location, end_location
        const [num, start, end]: number[] = trips[i];
    }

    // no problems, so it was successful carpool
    return true;
};
